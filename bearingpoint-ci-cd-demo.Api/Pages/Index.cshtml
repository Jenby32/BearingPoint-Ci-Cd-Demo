@page
@model IndexModel


<div class="text-center">
    <h1 class="display-4">Hello BearingPoint 👋</h1>
    <p class="lead">Willkommen auf meiner Bewerbungs-Demo mit .NET, CI/CD & Azure.</p>
</div>

<hr />

<section class="container" style="max-width:800px;margin:auto;text-align:left;">
    <h2>FAQ über mich</h2>

    <div class="accordion" id="faq">
        @for (int i = 0; i < Model.Faqs.Count; i++)
        {
            var item = Model.Faqs[i];
            var panelId = $"faq-panel-{i}";
            var btnId = $"faq-btn-{i}";
            <div class="accordion-item" style="margin-bottom:10px;">
                <button class="accordion-button"
                    id="@btnId"
                    aria-expanded="false"
                    aria-controls="@panelId"
                    data-target="#@panelId"
                    style="width:100%;text-align:left;padding:10px 12px;border:none;border-radius:6px;background-color:#f3f4f6;font-weight:600;cursor:pointer;">
                    @item.Question
                </button>
                <div class="accordion-panel" id="@panelId" role="region" aria-labelledby="@btnId" hidden
                 style="padding:10px 12px;background-color:#ffffff;border-radius:6px;border:1px solid #e5e7eb;">
                    <p>@item.Answer</p>
                </div>
            </div>
        }
    </div>
</section>

<hr />

<section id="ai" class="container" style="max-width:800px;margin:40px auto;text-align:left;">
    <h2 style="margin-bottom:10px;">Künstliche Intelligenz & LLM-Projekte</h2>

    <p style="color:#374151; line-height:1.7;">
        Außerdem interessiere ich mich stark für <strong>Künstliche Intelligenz</strong> und habe bereits
        praktische <strong>Erfahrung in der Implementierung von LLMs (Large Language Models)</strong> in mehreren Projekten gesammelt –
        unter anderem in meinem aktuellsten:
    </p>

    <h3 style="margin-top:20px;">Ziel des Projekts</h3>
    <p style="color:#374151;">
        Das Ziel war es, <strong>Unternehmen, Agenturen und Solo-Creators</strong> eine zentrale Plattform zu bieten,
        über die sie per <strong>API</strong> oder <strong>Dashboard</strong> auf mehrere KI-Funktionen zugreifen können.
        Im Fokus stand, reale Business-Prozesse mit modernen LLMs zu unterstützen und zu automatisieren.
    </p>

    <ul style="line-height:1.8;margin:16px 0 24px 20px;">
        <li><strong>Content planen</strong> – z. B. Social-Media- und Kampagnenplanung</li>
        <li><strong>Texte & Ads generieren</strong> – KI-gestützte Content-Erstellung</li>
        <li><strong>Content prüfen & verbessern</strong> – Grammatik, Stil, Tonalität, SEO</li>
        <li><strong>Workflows automatisieren</strong> – Freigaben, Reviews, Team-Kommentare</li>
        <li><strong>White-Label-Umgebungen</strong> – eigene Dashboards für Agenturen</li>
    </ul>

    <p style="color:#111827;font-weight:500;">
        <em>Kurz gesagt:</em> Ein <strong>KI-Toolkit</strong> für Marketing-, Kommunikations- und Automatisierungsaufgaben.
    </p>

    <hr style="margin:30px 0;" />

    <h3 style="margin-bottom:10px;">Technologischer Aufbau</h3>
    <p style="color:#374151;">
        Das Projekt besteht aus einem <strong>Next.js-Frontend in TypeScript</strong> und einem
        <strong>Python-Backend</strong>, das mit der <strong>OpenAI API</strong> kommuniziert.
        Der Fokus lag darauf, LLMs wie GPT in strukturierte, reproduzierbare Prozesse einzubetten –
        mit klar definierten Prompts, Ergebnis-Validierung und anpassbarer Business-Logik.
    </p>

    <ul style="line-height:1.7;margin-left:20px;">
        <li><strong>Frontend:</strong> Next.js (TypeScript) – intuitive UI, Auth, State Management</li>
        <li><strong>Backend:</strong> Python (FastAPI) – API-Endpunkte, Prompt-Verarbeitung & Ergebnis-Scoring</li>
        <li><strong>KI-Anbindung:</strong> OpenAI API – Textgenerierung, Datenanalyse, Prozessautomatisierung</li>
        <li><strong>CI/CD:</strong> GitHub Actions & Docker – automatisierter Build & Deployment-Flow</li>
    </ul>

    <div style="position:relative;padding-bottom:56.25%;height:0;overflow:hidden;border-radius:12px;box-shadow:0 8px 24px rgba(0,0,0,.15);margin-top:24px;">
        <iframe src="https://www.youtube.com/embed/G_u8G8GlrGE"
                title="AI Toolkit - Kevin Kozar"
                allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
                allowfullscreen
                style="position:absolute;top:0;left:0;width:100%;height:100%;border:0;border-radius:12px;">
        </iframe>
    </div>

    <p style="color:#6b7280;margin-top:10px;">
        Im Video erkläre ich (bzw. eine KI generierte Stimme) die Idee, und präsentiere das AI Toolkit.
    </p>
</section>


@section Scripts {
    <script>
        document.querySelectorAll('.accordion-button').forEach(btn => {
          btn.addEventListener('click', () => {
            const target = document.querySelector(btn.getAttribute('data-target'));
            const expanded = btn.getAttribute('aria-expanded') === 'true';
            btn.setAttribute('aria-expanded', String(!expanded));
            target.hidden = expanded;
          });
        });
    </script>
}
